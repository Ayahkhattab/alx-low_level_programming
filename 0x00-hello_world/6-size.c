#include <stdio.h>
/**
 * main - A program that prints a line using the size of various computer types
 * Return: 0 (Success)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	float f;
printf("size of a char: %lu byte(s)\n",(unsigned long)sizeof(a));		
return (0);
}
